# Exemplo de .env para o projeto mark85-cypress-api
# Copie este arquivo para .env e preencha os valores antes de rodar os testes localmente

BASE_URL=http://localhost:3333
MONGO_URI=mongodb://localhost:27017/markdb

# CloudAMQP example (caso use CloudAMQP)
AMQP_HOST=https://<your-cloudamqp-host>/api/queues/<vhost>
AMQP_QUEUE=tasks
AMQP_TOKEN=Basic <base64_token_without_quotes>

# Observações:
# - Não coloque aspas em AMQP_TOKEN (evita problemas ao parsear o valor).
# - Se preferir, defina esses valores como Secrets no GitHub Actions (recomendado para CI).
